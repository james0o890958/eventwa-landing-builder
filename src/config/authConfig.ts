/**
 * Global Authentication Configuration
 * Toggle AUTH_ENABLED to true/false to turn auth protection on or off.
 */
export const AUTH_CONFIG = {
  AUTH_ENABLED: true, // Set to true to enable real Supabase auth, false to bypass
};
